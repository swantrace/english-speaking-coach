# Local development calls the backend directly.
VITE_API_BASE_URL=http://localhost:3001

# On Vercel, omit VITE_API_BASE_URL so requests use the same-origin /api proxy
# configured in vercel.json. LiveKit connection details come from the backend;
# no LiveKit secret or provider API key belongs in the browser environment.
